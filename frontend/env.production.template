# ============================================
# Frontend Production Environment Variables Template
# ============================================
# Copy this file to .env.production and fill in your actual values
# DO NOT commit .env.production to version control (it's in .gitignore)
#
# Command to create: cp env.production.template .env.production

# ============================================
# REQUIRED VARIABLES
# ============================================

# Backend API URL
# This is the URL where your backend API is hosted
# For Render deployment: https://stress-relief-backend.onrender.com
# For custom domain: https://api.yourdomain.com
NEXT_PUBLIC_API_URL=https://stress-relief-backend.onrender.com

# Google OAuth Client ID (Public)
# Get this from Google Cloud Console: https://console.cloud.google.com/
# This is exposed to the browser, so it's safe to be public
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com

# Google OAuth Client Secret (Server-side only)
# Get this from Google Cloud Console
# This is used in API routes and should NOT be exposed to the browser
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# MongoDB Connection String (Optional - only if frontend needs direct DB access)
# Usually only needed if you have server-side API routes that connect to MongoDB
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/stressrelief?retryWrites=true&w=majority

# ============================================
# OPTIONAL VARIABLES
# ============================================

# Node Environment (usually set automatically by Next.js)
# Set to 'production' for production builds
NODE_ENV=production

# Build ID (optional, for build tracking)
# Used for cache busting and build identification
BUILD_ID=production

# ============================================
# PAYMENT INTEGRATION (Optional - if using Razorpay)
# ============================================
# RAZORPAY_KEY_ID=your_razorpay_key_id
# RAZORPAY_KEY_SECRET=your_razorpay_key_secret

# ============================================
# VOICE CALL INTEGRATION (Optional - if using Twilio)
# ============================================
# TWILIO_ACCOUNT_SID=your_twilio_account_sid
# TWILIO_AUTH_TOKEN=your_twilio_auth_token

# ============================================
# NOTES
# ============================================
# 1. Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# 2. Variables without NEXT_PUBLIC_ are server-side only
# 3. Never commit sensitive values to version control
# 4. For Render deployment, set these in the Render dashboard Environment section
# 5. The backend URL should match your backend service URL exactly



